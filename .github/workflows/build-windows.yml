name: Legacy ZIP Image to PDF v2 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy v2 build is disabled. Use build-windows-electron.yml."
