# Campus QA Web App

This project provides a minimal Flask backend with a simple HTML/JavaScript frontend. Users can register, log in, and chat with a placeholder campus QA agent.

## Setup

Install dependencies:
```bash
pip install -r requirements.txt
```

Run the server:
```bash
python backend/app.py
```

Open `frontend/index.html` in your browser to use the application.
